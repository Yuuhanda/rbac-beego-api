$kind: http-request
name: Remove Role from User
method: DELETE
url: "{{baseUrl}}/api/user-roles/:userId/:roleId"
order: 4000
headers:
  Authorization: "{{authToken}}"
pathVariables:
  userId: "13"
  roleId: admin
