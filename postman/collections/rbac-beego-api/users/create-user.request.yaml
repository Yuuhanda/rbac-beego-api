$kind: http-request
name: Create User
method: POST
url: "{{baseUrl}}/user"
order: 1000
headers:
  Content-Type: application/json
  Authorization: "{{authToken}}"
body:
  type: json
  content: |-
    {
      "username": "johndoe2",
      "email": "john2@example.com",
      "password": "password123"
    }
