$kind: http-request
name: Get User Visits
method: GET
url: "{{baseUrl}}/user/:id/visits"
order: 6000
headers:
  Authorization: "{{authToken}}"
pathVariables:
  id: "2"
queryParams:
  page: "1"
  page_size: "10"
