$kind: http-request
name: List Auth Items
method: GET
url: "{{baseUrl}}/api/auth-items?page=1&page_size=30"
order: 2000
headers:
  Authorization: "{{authToken}}"
queryParams:
  page: "1"
  page_size: "30"
