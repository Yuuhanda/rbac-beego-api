$kind: http-request
name: Create Auth Item
method: POST
url: "{{baseUrl}}/api/auth-items"
order: 1000
headers:
  Content-Type: application/json
  Authorization: "{{authToken}}"
body:
  type: json
  content: |-
    {
      "Role": "admin",
      "Path": "/api/routes/list",
      "Method": "GET"
    }
